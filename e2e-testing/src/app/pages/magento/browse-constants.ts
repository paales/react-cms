/** Catalog page geometry, shared by the route (server), the product
 *  grid, and the client-rendered skeleton. */
export const PAGE_SIZE = 12
export const GRID = "grid flex-1 grid-cols-4 grid-rows-3 gap-3 min-h-0"
