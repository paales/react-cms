{
  // Root tsconfig is a placeholder — actual compilation is per-workspace
  // via each package's tsconfig.json. Each workspace extends
  // ./tsconfig.base.json and declares its own paths. The IDE picks up
  // each workspace independently when its files are opened.
  "extends": "./tsconfig.base.json",
  "files": [],
  "include": []
}
